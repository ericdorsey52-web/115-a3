# 🏗️ Project Architecture & Diagram

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                     DJANGO BLOG PLATFORM                        │
│                    (Full-Stack Application)                      │
└─────────────────────────────────────────────────────────────────┘

                    FRONTEND LAYER (User Facing)
┌──────────────────────────────────────────────────────────────────┐
│  HTML Templates + CSS + JavaScript                               │
│  ┌──────────────┐ ┌────────────┐ ┌──────────┐ ┌────────────┐    │
│  │  Sign Up     │ │   Log In   │ │   Home   │ │ Blog Post  │    │
│  │   Page       │ │    Page    │ │   Page   │ │   Detail   │    │
│  └──────────────┘ └────────────┘ └──────────┘ └────────────┘    │
│                                                                   │
│  CSS Design System: design_system.css                            │
│  ├── Colors (Orange, Blue, Gray)                               │
│  ├── Components (Buttons, Inputs, Cards)                       │
│  ├── Typography (Headings, Body, Labels)                       │
│  └── Responsive Layout (Mobile, Tablet, Desktop)               │
└──────────────────────────────────────────────────────────────────┘
                            ↕↕↕
                    APPLICATION LAYER (Django)
┌──────────────────────────────────────────────────────────────────┐
│  Django Views & Forms                                            │
│                                                                   │
│  ┌────────────────┐  ┌────────────────┐  ┌──────────────────┐   │
│  │  Views (views) │  │ Forms (forms)  │  │ URLs (urls)      │   │
│  ├─ signup_view  │  ├─ UserSignUp    │  ├─ /signup/        │   │
│  ├─ login_view   │  ├─ UserLogin     │  ├─ /login/         │   │
│  ├─ home_view    │  ├─ BlogPostForm  │  ├─ /               │   │
│  ├─ create_blog  │  └─────────────┘  ├─ /post/<id>/        │   │
│  └─ blog_detail  │                   ├─ /create/           │   │
│                                       └─ /about/            │   │
└──────────────────────────────────────────────────────────────────┘
                            ↕↕↕
                    DATA LAYER (Models & ORM)
┌──────────────────────────────────────────────────────────────────┐
│  Django ORM Models                                               │
│                                                                   │
│  ┌──────────────────┐           ┌──────────────────┐            │
│  │   UserProfile    │           │    BlogPost      │            │
│  ├──────────────────┤           ├──────────────────┤            │
│  │ - user (FK)      │           │ - author (FK)    │            │
│  │ - bio            │           │ - title          │            │
│  │ - avatar_url     │           │ - content        │            │
│  │ - created_at     │           │ - excerpt        │            │
│  │ - updated_at     │           │ - created_at     │            │
│  └──────────────────┘           │ - updated_at     │            │
│          ▲                       │ - published      │            │
│          │                       │ - views_count    │            │
│          │ (1:1)                 └──────────────────┘            │
│      ┌───┴─────────────┐                ▲                        │
│      │   Django User   │                │ (1:N)                  │
│      │     (Built-in)  │                │                        │
│      └─────────────────┘                │                        │
└──────────────────────────────────────────────────────────────────┘
                            ↕↕↕
                    DATABASE (SQLite)
┌──────────────────────────────────────────────────────────────────┐
│  SQLite Database (db.sqlite3)                                    │
│  Tables:                                                          │
│  ├─ auth_user (Django built-in)                                 │
│  ├─ blog_userprofile (Extended user data)                       │
│  ├─ blog_blogpost (Blog articles)                               │
│  └─ ... (other Django tables)                                   │
└──────────────────────────────────────────────────────────────────┘
```

---

## Request-Response Flow

### Example: User Sign Up

```
1. USER INTERACTION
   ┌─────────────────────────────────────────┐
   │ User visits /signup/                    │
   │ Browser sends: GET /signup/             │
   └─────────────────────────────────────────┘
           ↓
2. DJANGO ROUTING
   ┌─────────────────────────────────────────┐
   │ urls.py matches GET /signup/            │
   │ Routes to: signup_view()                │
   └─────────────────────────────────────────┘
           ↓
3. VIEW PROCESSING
   ┌─────────────────────────────────────────┐
   │ signup_view() in views.py               │
   │ GET request? → Show empty form          │
   │ Creates UserSignUpForm instance         │
   └─────────────────────────────────────────┘
           ↓
4. TEMPLATE RENDERING
   ┌─────────────────────────────────────────┐
   │ Renders auth/signup.html                │
   │ Injects form fields, buttons, styles    │
   │ Uses CSS classes from design_system.css │
   └─────────────────────────────────────────┘
           ↓
5. BROWSER DISPLAYS
   ┌─────────────────────────────────────────┐
   │ HTML + CSS rendered in browser          │
   │ User sees: Form with styled fields      │
   │ Design System: Orange button, gray      │
   │ inputs with blue focus states           │
   └─────────────────────────────────────────┘

─────── USER FILLS FORM & SUBMITS ───────

6. FORM SUBMISSION
   ┌─────────────────────────────────────────┐
   │ User clicks "Create Account"            │
   │ Browser sends: POST /signup/            │
   │ With form data (name, email, password)  │
   └─────────────────────────────────────────┘
           ↓
7. FORM VALIDATION
   ┌─────────────────────────────────────────┐
   │ UserSignUpForm.is_valid()               │
   │ Checks:                                 │
   │ - Email is unique                       │
   │ - Passwords match                       │
   │ - Password is strong enough             │
   │ - Required fields filled                │
   └─────────────────────────────────────────┘
           ↓
8. DATABASE SAVE
   ┌─────────────────────────────────────────┐
   │ form.save()                             │
   │ Creates new Django User in database     │
   │ Saves to: auth_user table               │
   └─────────────────────────────────────────┘
           ↓
9. SESSION & REDIRECT
   ┌─────────────────────────────────────────┐
   │ login(request, user)                    │
   │ Creates session for logged-in user      │
   │ Redirect to home page                   │
   └─────────────────────────────────────────┘
           ↓
10. SUCCESS MESSAGE
    ┌─────────────────────────────────────────┐
    │ message.success(): "Account created!"   │
    │ Displayed in alert on homepage          │
    │ Uses: .alert .alert-success styles      │
    └─────────────────────────────────────────┘
```

---

## File Communication Map

```
USER INTERACTION
       ↓
    ┌──────────────────────────────────────────┐
    │ Browser (HTML/CSS/JavaScript)            │
    └──────────────────────────────────────────┘
       ↓ HTTP Request (GET/POST)
    ┌──────────────────────────────────────────┐
    │ urls.py (Route Mapping)                  │
    │ /signup/ → signup_view()                 │
    │ /login/ → login_view()                   │
    │ /post/<id>/ → blog_detail_view()         │
    └──────────────────────────────────────────┘
       ↓
    ┌──────────────────────────────────────────┐
    │ views.py (Page Logic & Processing)       │
    │ - Handle GET/POST requests               │
    │ - Create/validate forms                  │
    │ - Query database                         │
    │ - Render templates                       │
    └──────────────────────────────────────────┘
       ↓ Uses
    ┌──────────────────────────────────────────┐
    │ forms.py (Data Validation)               │
    │ - UserSignUpForm                         │
    │ - UserLoginForm                          │
    │ - BlogPostForm                           │
    └──────────────────────────────────────────┘
       ↓ Queries/Saves
    ┌──────────────────────────────────────────┐
    │ models.py (Data Definition)              │
    │ - UserProfile                            │
    │ - BlogPost                               │
    └──────────────────────────────────────────┘
       ↓ Reads/Writes
    ┌──────────────────────────────────────────┐
    │ Database (SQLite)                        │
    │ db.sqlite3                               │
    └──────────────────────────────────────────┘

    ← Returns data and renders
    
    ┌──────────────────────────────────────────┐
    │ templates/*.html (HTML Structure)        │
    │ - Injects data from views                │
    │ - Includes CSS classes                   │
    │ - Renders form fields                    │
    └──────────────────────────────────────────┘
       ↓ Uses
    ┌──────────────────────────────────────────┐
    │ design_system.css (Visual Design)        │
    │ - Colors (#FF6B35, #006BFF)              │
    │ - Styles (.btn-primary, .form-control)  │
    │ - Typography (font sizes, weights)       │
    │ - Responsive layout                      │
    └──────────────────────────────────────────┘
       ↓ HTTP Response (HTML + CSS)
    ┌──────────────────────────────────────────┐
    │ Browser Renders Final Page               │
    │ User sees styled, interactive page       │
    └──────────────────────────────────────────┘
```

---

## Component Hierarchy

```
Page (HTML Document)
│
├─ Header
│  ├─ Logo
│  └─ Navigation (nav-links)
│     ├─ Home Link
│     ├─ About Link
│     ├─ User Menu
│     └─ Sign Up Button (.btn-primary)
│
├─ Messages/Alerts
│  ├─ .alert-success
│  ├─ .alert-error
│  └─ .alert-info
│
├─ Main Content
│  │
│  ├─ (For Sign Up Page)
│  │  └─ Form Container (.form-container)
│  │     ├─ Form Header
│  │     ├─ Form Groups (.form-group)
│  │     │  ├─ Label (.form-label)
│  │     │  ├─ Input (.form-control)
│  │     │  └─ Error Message (.form-error)
│  │     ├─ Submit Button (.btn-primary)
│  │     └─ Form Footer (Login Link)
│  │
│  ├─ (For Blog Homepage)
│  │  └─ Section (.section)
│  │     ├─ Section Title (.section-title)
│  │     └─ Card List
│  │        └─ Card (.card) × N
│  │           ├─ Card Header
│  │           │  ├─ Avatar (.shape-circle)
│  │           │  ├─ Title (.card-title)
│  │           │  └─ Subtitle (.card-subtitle)
│  │           ├─ Card Content (.card-content)
│  │           └─ Card Footer (.card-footer)
│  │              ├─ View Count
│  │              └─ Read More Button
│  │
│  └─ (For Blog Detail Page)
│     ├─ Article Header
│     │  ├─ Title
│     │  ├─ Author Avatar (.shape-circle)
│     │  ├─ Author Name
│     │  ├─ Publication Date
│     │  └─ View Count
│     ├─ Article Content
│     └─ Back Button
│
└─ Footer
   └─ Copyright Text
```

---

## User Authentication Flow

```
┌─────────────────────────────────────────────────────────┐
│              AUTHENTICATION FLOW DIAGRAM                 │
└─────────────────────────────────────────────────────────┘

SIGN UP FLOW:
═════════════

  1. User visits /signup/
       ↓
  2. Views show signup.html (empty form)
       ↓
  3. User fills form & submits POST
       ↓
  4. UserSignUpForm validates data
       ├─ Check email unique
       ├─ Check passwords match
       └─ Check password strength
       ↓ (If valid)
  5. Create Django User in database
       ↓
  6. Auto-login user: login(request, user)
       ↓
  7. Redirect to home
       ↓
  8. Display success message
       ↓
  9. User is authenticated (session created)


LOG IN FLOW:
════════════

  1. User visits /login/
       ↓
  2. Views show login.html (form)
       ↓
  3. User fills username/password & submits
       ↓
  4. authenticate(username, password)
       ├─ Query database for user
       └─ Check password (Django hashes)
       ↓ (If valid)
  5. login(request, user) - create session
       ↓
  6. Redirect to home
       ↓
  7. Display success message
       ↓
  8. User is authenticated for all requests


PROTECTED PAGE FLOW:
════════════════════

  1. User tries to access /create/ (post creation)
       ↓
  2. Check: @login_required decorator
       ├─ User authenticated? YES
       │  └─ Show page
       └─ User authenticated? NO
          └─ Redirect to /login/
       ↓
  3. User completes action (create post)
       ↓
  4. Data saved to database with user as author


LOG OUT FLOW:
═════════════

  1. User clicks "Log Out"
       ↓
  2. logout(request)
       ├─ Delete session
       └─ Clear cookies
       ↓
  3. Redirect to home
       ↓
  4. Display logout success message
       ↓
  5. User no longer authenticated
       ↓
  6. Clicking protected pages redirects to login
```

---

## Database Schema Relationship

```
┌─────────────────────────────────────────────────────────┐
│              DATABASE RELATIONSHIPS                      │
└─────────────────────────────────────────────────────────┘

            Django Built-in: auth_user
          ┌──────────────────────────────┐
          │         User                 │
          ├──────────────────────────────┤
          │ id (Primary Key)             │
          │ username (Unique)            │
          │ email (Unique)               │
          │ password (Hashed)            │
          │ first_name                   │
          │ last_name                    │
          │ is_active                    │
          │ is_staff                     │
          │ is_superuser                 │
          │ created_at                   │
          └──────────────────────────────┘
                      │
                      │ (1:1 relationship)
                      │ OneToOneField
                      │
                      ▼
          ┌──────────────────────────────┐
          │    UserProfile (Custom)      │
          ├──────────────────────────────┤
          │ id (Primary Key)             │
          │ user_id (Foreign Key to User)│ (Unique)
          │ bio (Text)                   │
          │ avatar_url (URL)             │
          │ created_at                   │
          │ updated_at                   │
          └──────────────────────────────┘


            ┌──────────────────────────────┐
            │   BlogPost                   │
            ├──────────────────────────────┤
            │ id (Primary Key)             │
            │ author_id (FK to User)       │ ◄──┐
            │ title (Unique)               │    │
            │ content (Text)               │    │ (1:N)
            │ excerpt (Char)               │    │ Foreign Key
            │ created_at                   │    │
            │ updated_at                   │    │
            │ published (Boolean)          │    │
            │ views_count (Integer)        │    │
            └──────────────────────────────┘    │
                                                 │
                      ┌──────────────────────────┘
                      │ One User can write many
                      │ BlogPosts


┌─────────────────────────────────────────────────────────┐
│              DATA FLOW EXAMPLE                           │
└─────────────────────────────────────────────────────────┘

1. User Signs Up
   auth_user table gets new row:
   ┌──────────┬──────────┬────────────────────┐
   │ id       │ username │ email              │
   ├──────────┼──────────┼────────────────────┤
   │ 1        │ johndoe  │ john@example.com   │
   └──────────┴──────────┴────────────────────┘

2. User Profile Created (auto)
   blog_userprofile table gets row:
   ┌────────────┬──────────────┬──────────┐
   │ id         │ user_id (FK) │ bio      │
   ├────────────┼──────────────┼──────────┤
   │ 1          │ 1            │ ''       │
   └────────────┴──────────────┴──────────┘

3. User Creates Blog Post
   blog_blogpost table gets row:
   ┌────────────┬────────────────┬──────────────┐
   │ id         │ author_id (FK) │ title        │
   ├────────────┼────────────────┼──────────────┤
   │ 1          │ 1              │ My First Post│
   └────────────┴────────────────┴──────────────┘

4. Blog Post is displayed:
   Query: SELECT * FROM blog_blogpost WHERE id=1
   Join: Get author info from auth_user WHERE id=1
   Display: "By johndoe - john@example.com"
```

---

## Deployment Architecture (Future)

```
┌────────────────────────────────────────────────────┐
│         PRODUCTION DEPLOYMENT STACK                │
│                  (Example Setup)                   │
└────────────────────────────────────────────────────┘

User's Browser
      │
      │ (HTTPS)
      ▼
┌────────────────────────────────────────────────────┐
│  Web Server (Nginx / Apache)                       │
│  ├─ Serves static files (CSS, images)             │
│  └─ Proxies requests to application               │
└────────────────────────────────────────────────────┘
      │
      │ (Local)
      ▼
┌────────────────────────────────────────────────────┐
│  Application Server (Gunicorn / uWSGI)             │
│  ├─ Runs Django application                       │
│  ├─ Handles multiple concurrent requests          │
│  └─ Manages worker processes                      │
└────────────────────────────────────────────────────┘
      │
      │
      ▼
┌────────────────────────────────────────────────────┐
│  Django Application                                │
│  ├─ views.py (business logic)                     │
│  ├─ models.py (data layer)                        │
│  └─ forms.py (validation)                         │
└────────────────────────────────────────────────────┘
      │
      │
      ▼
┌────────────────────────────────────────────────────┐
│  Database Server (PostgreSQL / MySQL)              │
│  ├─ Stores user data                              │
│  ├─ Stores blog posts                             │
│  └─ Handles data persistence                      │
└────────────────────────────────────────────────────┘

Additional Services:
├─ Email Server (for notifications)
├─ Cache Layer (Redis for performance)
├─ CDN (for static files)
├─ SSL Certificate (for HTTPS)
└─ Monitoring Tools (for uptime/errors)
```

---

## Component to CSS Mapping

```
Figma Component          CSS Class           HTML Element
═══════════════════════════════════════════════════════════

Button/Primary     →     .btn-primary    →   <button>
Button/Secondary   →     .btn-secondary  →   <button>
Button/Tertiary    →     .btn-tertiary   →   <a>

Input/TextField    →     .form-control   →   <input>
Input/Label        →     .form-label     →   <label>
Input/Error        →     .form-error     →   <span>

Shape/Circle       →     .shape-circle   →   <div>
Shape/Square       →     .shape-square   →   <div>

Card               →     .card           →   <div>
Card/Header        →     .card-header    →   <div>
Card/Content       →     .card-content   →   <div>
Card/Footer        →     .card-footer    →   <div>

Alert/Success      →     .alert-success  →   <div>
Alert/Error        →     .alert-error    →   <div>

Navigation         →     .nav-links      →   <ul>
Container          →     .container      →   <div>
Section            →     .section        →   <section>
```

---

## Development Workflow

```
DEVELOPER WORKFLOW FOR THIS PROJECT:
═══════════════════════════════════════════════════════════

1. DESIGN PHASE (Figma)
   ↓
   ├─ Create components in Figma
   ├─ Define color palette
   ├─ Create wireframes
   └─ Create high-fidelity designs

2. FRONTEND IMPLEMENTATION
   ↓
   ├─ Create design_system.css with all styles
   ├─ Convert Figma colors → CSS variables
   ├─ Convert components → CSS classes
   └─ Create HTML templates with classes

3. BACKEND IMPLEMENTATION
   ↓
   ├─ Define models in models.py
   ├─ Create forms in forms.py
   ├─ Write views in views.py
   └─ Set up URLs in urls.py

4. INTEGRATION
   ↓
   ├─ Connect templates to views
   ├─ Test form submission
   ├─ Verify styling matches Figma
   └─ Test responsive design

5. TESTING
   ↓
   ├─ Manual user testing
   ├─ Cross-browser testing
   ├─ Mobile device testing
   └─ Form validation testing

6. DEPLOYMENT
   ↓
   ├─ Set up production server
   ├─ Configure database
   ├─ Deploy code
   └─ Monitor performance
```

---

**This diagram shows the complete architecture from user interaction through to database storage.**

For more details on specific components, see:
- **COMPONENT_REFERENCE.md** - All components and classes
- **design_system.css** - Implementation details
- **views.py, models.py** - Backend implementation
